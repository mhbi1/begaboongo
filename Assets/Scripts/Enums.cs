public enum CardType {
    Attack = 0,
    Spell = 1,
    Enchantment = 2
}
public enum ActionTypes {
    Attack = 0,
    Spell = 1,
    Enchantment = 2
}

public enum CardRarity {
    Common = 0,
    Uncommon = 1,
    Rare = 2
}

public enum PartyMemberClass {
    AttackPerson = 0,
    SpellPerson = 1,
    EnchantmentPerson = 2
}